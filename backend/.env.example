PORT=4000
NODE_ENV=development
# 1: crea usuarios demo (admin@demo, prof@demo, alum@demo)
# 0: nunca crea usuarios demo
SEED_DEMO_USERS=1
# En produccion, aunque valga 1, los usuarios demo se deshabilitan.
# Si no existe ningun ADMIN en produccion, se crea admin@ucm con password aleatoria
# y se deja en backend/tmp/bootstrap-admin.txt (eliminalo tras primer acceso por seguridad).
